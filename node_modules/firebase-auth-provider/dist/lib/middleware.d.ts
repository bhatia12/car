import { IAuthProvider } from './auth_provider';
export declare abstract class FirebaseAuthMiddleware {
    private auth;
    protected excludedPaths: string[];
    protected constructor(auth: IAuthProvider);
    getMiddleware(): (req: any, res: any, next: any) => void;
    handle(req: any, res: any, next: any): Promise<any>;
    protected readTokenFromRequest(req: any): string;
    abstract findUser(uid: string): any;
    abstract createError(msg: string): Error;
}
